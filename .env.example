# Proxy Configuration (Optional)
# Uncomment and configure if you need to use a proxy
# Note: SOCKS proxy takes precedence over HTTP proxy if both are set

# ===== SOCKS5 Proxy =====
# SOCKS_PROXY=socks5://username:password@proxy-host:port
# SOCKS5_PROXY=socks5://username:password@proxy-host:port

# Examples:
# Without authentication:
# SOCKS_PROXY=socks5://127.0.0.1:1080
# SOCKS5_PROXY=socks5://proxy.example.com:1080

# With basic authentication:
# SOCKS_PROXY=socks5://myuser:mypass@127.0.0.1:1080
# SOCKS5_PROXY=socks5://myuser:mypass@proxy.example.com:1080

# ===== HTTP/HTTPS Proxy =====
# HTTP_PROXY=http://username:password@proxy-host:port
# HTTPS_PROXY=http://username:password@proxy-host:port

# Examples:
# Without authentication:
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# With basic authentication:
# HTTP_PROXY=http://myuser:mypass@proxy.example.com:8080
# HTTPS_PROXY=http://myuser:mypass@proxy.example.com:8080
